# Testing (superseded)

**This file was stale** (it described a repo with no tests and no Jest setup). As of 2026, tests exist and configuration lives in the app repo.

**Use instead:** **[`docs/TESTING_TEMPLATES.md`](../../docs/TESTING_TEMPLATES.md)** — Jest (`templatesLoader`, `templatesCache`), guard tests (no runtime template loaders), E2E notes, commands (`npm test`, `npm run test:e2e`).
