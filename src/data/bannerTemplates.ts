import type { UnitTemplate } from '../models/UnitTemplate';

/**
 * Banner templates are loaded from titan-data (root templates.json) via hooks/cache.
 * via the single templates loader and useBannerTemplates().
 * This stub is kept for any legacy imports; prefer useBannerTemplates() in screens.
 */
export const bannerTemplates: UnitTemplate[] = [];
